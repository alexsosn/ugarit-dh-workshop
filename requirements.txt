# Core scientific stack — used by every notebook
numpy
pandas
matplotlib
scikit-learn      # TF-IDF, cosine similarity, clustering
scipy
plotly            # interactive maps in notebook 2b
umap-learn; python_version < "3.14"  # headline genre map; PCA fallback on newer Python

# Networks (Hour 3 — letters as social graphs)
networkx

# Decision trees / divination structures (Hour 3)
# scikit-learn covers modeling; graphviz is optional for nicer tree rendering
graphviz

# Notebook environment
jupyterlab
ipykernel

# Optional Hour-3 PDF → local SQLite exercise
pdfminer.six>=20240706

# OPTIONAL — full Text-Fabric access to the original CUC graph dataset.
# The default loader uses HuggingFace JSONL via the Python standard library.
# text-fabric
